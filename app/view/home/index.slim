doctype
html
  head
    meta charset='utf-8'
    title Living on the edge
    css:
      html, body {
        width: 100%;
        height: 100%;
        overflow: hidden;
        background-color: #efefef;
      }

      h1 {
        position: absolute;
        font-size: #{@title_size}px;
        color: #444;
        font-weight: bold;
        font-family: serif;
        width: 100%;
        text-align: center;
        top: 50%;
        margin-top: -0.5em;
        line-height: 1;
        text-shadow: 1px 1px 1px rgba(0, 0, 0, 0.3);
      }

      #message {
        position: absolute;
        top: 50%;
        margin-top: #{@title_size}px;
        color: #333;
        width: 100%;
        text-align: center;
        line-height: 1;
        font-size: 60px;
        font-size: serif;
        text-shadow: 0 1px 1px rgba(0, 0, 0, 0.3);
      }
  body
    h1 Living on the Edge
    p#message= @message
